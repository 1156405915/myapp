# 输出格式

## 表格字段

| 字段 | 原始值 | 规范化值 | 来源定位 | 状态 | 备注 |
|---|---|---|---|---|---|

状态只能使用：`明确`、`冲突`、`未找到`、`不适用`、`无法识别`。

## JSON 字段

每项使用以下结构：

```json
{
  "value": null,
  "normalizedValue": null,
  "source": "",
  "status": "未找到",
  "note": ""
}
```

不要将未知值填成零、空字符串或推测值，除非用户明确指定默认值。
